# Очередь сдачи INF-13 / INF-15

Служебный журнал очереди сдачи в `master` (раздел 10 обоих ТЗ). Правила:

- Сдача — по одной за раз, через замок `handoff/09_next_research/LOCKS/merge.lock`
  (содержимое: `<INF-код> <ISO-время захвата> <PR#ии-номер коммита>`). Просрочка
  замка — 45 минут; после просрочки следующая группа занимает очередь и
  записывает это ниже.
- Мерж — squash, сообщение коммита `INF-13 <WP>: <суть>` / `INF-15 <WP>: <суть>`.
- После каждого мержа — запись строкой в таблицу ниже (сама группа, вручную).
- Общие файлы — только `append` с маркерами `/* INF-13 BEGIN */ … /* INF-13 END */`
  или `/* INF-15 BEGIN */ … /* INF-15 END */`. При коллизии (обе группы задели
  один файл) — сохраняются оба блока, порядок по номеру исследования (INF-13
  выше INF-15).
- Публикация (деплой) — свой отдельный замок `LOCKS/deploy.lock`, таймаут 30
  минут; не путать с замком сдачи.

## Журнал сдач

| # | Дата (ISO) | Группа | WP / этап | Коммит/PR | Что сдано | Замок держался |
|---|---|---|---|---|---|---|
| 1 | 2026-07-26T18:35:00+03:00 | INF-13 | этап 0 (подготовка) | `3d23181` (master, squash от `feature/inf13-pension`@`bb661a5`) | пререгистрация (`docs/preregistration/pension-v0.1.md`), районная возрастная деталь прогноза (`forecast_age_raion_v2026_4.json`), параметризация `E2E_PORT` в `web/playwright.config.ts`, служебные файлы регламента (этот файл, `LOCKS/`, `docs/decisions/INF-13.md`, `docs/decisions/INF-15.md`) | ~2 мин, снят после мержа |
| 2 | 2026-07-26T22:40:00+03:00 | INF-13 | этапы 1-4 (расчёт, архив, страница, e2e) | `89a9215` (master, squash от `feature/inf13-pension`) | `etl/pension.py` (SR/TDR/crossing_year/CG, 7 проверок), `artifacts/pension/` (`by-maps-pension-v1.0.0.zip`), методблок RU/BE, `/research/pension` + `/be` + `/artifacts/pension` + `/be`, `web/e2e/pension.spec.ts`. Известный побочный эффект этапа 0 (поломка байт-репродукции `forecast`/`mlchallenger` — вендор-код `sub.py`/`run.py`) зафиксирован в `docs/decisions/INF-13.md`, вынесен отдельной задачей, не входит в эту сдачу. | ~15 мин, снят после мержа |
| 3 | 2026-07-26T21:37:30+03:00 | INF-15 | этапы 1-8 (вся работа целиком) | `c4603f4` (master, squash от `feature/inf15-grid`) | Исследование «Полотно» целиком: `/research/grid` + `/be/research/grid`, `by-maps-grid-v1.0.0.zip`, методблок RU/BE, рилс RU/BE, e2e (26/26). Находки: G-1 (сырой GHS-POP) не пройдена → устранена constrained-калибровкой (D-005, ревью 3 ролей); G-3 (ночные огни) не пройдена (32.8%/70%) → C-1/C-2 поданы как открытый вопрос (D-007); G-2/G-4/G-5/G-6/C-3 пройдены. D-008: обнаружен предсуществующий красный CI у `by-maps-forecast` (не наш файл, не трогали) — см. `docs/decisions/INF-15.md`. | ~5 мин, снят после мержа |
| 4 | 2026-07-26T23:05:00+03:00 | INF-13 | этап 6 (рилс + итоговый отчёт) — последняя сдача | `8f96ff4` (master, squash от `feature/inf13-pension`) | `tools/render_reel_pension.py` + сцена (RU/BE mp4, не версионируются), `etl/tests/test_pension_reel.py`, `handoff/09_next_research/INF-13_REPORT.md` (итоговый отчёт с чеклистом раздела 9). Исследование INF-13 завершено целиком. | ~10 мин, снят после мержа |
| 5 | 2026-07-27T09:03:00+03:00 | INF-15 | финальная сдача — итоговый отчёт | `de8458a` (master, cherry-pick от `feature/inf15-grid`@`d95b6a1`) | `handoff/09_next_research/INF-15_REPORT.md` (чеклист раздела 9 — 17/17 «да») + закрытие журнала решений `docs/decisions/INF-15.md`. Исследование INF-15 завершено целиком (этапы 1-8, публикация и проверка после публикации — см. отчёт). | ~2 мин, снят после мержа |
| 6 | 2026-07-27T10:35:01+03:00 | INF-13 | UX/копирайтинг `/research/pension` (по `handoff/10_pension_ux_copy/INF-13_PENSION_UX_AND_COPY_v1.md`) | `6dc5803` (master, squash от `feature/inf13-pension-ux`) | Два слоя текста «Просто/Профессионально» (`?copy=pro`, контент-файлы `web/public/content/research/pension.{simple,pro}.{ru,be}.md`), новая компоновка десктопа (карта → время → легенда → счётчик → настройки → карточка района → график страны → находки), мобильный аккордеон/шторка/липкая панель с play, `web/e2e/pension.spec.ts` расширен. Расчёт/данные/архив/методблок не изменены (checksum `073182a3…` совпадает до/после). Отчёт: `handoff/10_pension_ux_copy/INF-13_UX_REPORT.md`. | ~1 час, снят после мержа |
| 7 | 2026-07-27T10:52:00+03:00 | INF-13 | хотфикс — поле «Год рождения» невидимо | `c8fd52b` (master) | Предсуществующий баг с самой первой сдачи (`89a9215`): `select, input { color: inherit }` даёт белый текст в тёмной теме без явного фона инпута → белый на белом, ввод невозможно увидеть. Найден пользователем на живой странице. Исправлено (`.pen-birth-input` — явные background/color/border), проверено в тёмной/светлой теме, e2e зелёный (71/71). | ~5 мин, снят после мержа |
| 8 | 2026-07-27T11:05:00+03:00 | INF-13 | хотфикс — сжатие кнопок/слайдера на средних ширинах экрана | `ae7587c` (master) | Найдено пользователем на живой странице (~800px): `.control-group` и `.pen-timebar` переносили содержимое на новую строку только при ширине ≤480px/≤640px — новые элементы (`.pen-setting-hint`, кнопка ▶ в одной строке со слайдером) на промежуточных ширинах сжимали соседей («негативный»/«скорректированный» обрезались, 9 меток годов слайдера налезали друг на друга). Исправлено — пояснения и слайдер переносятся на свою строку при нехватке места, вместо сжатия на месте; проверено на 375/800/1280px, e2e зелёный (71/71). | ~10 мин, снят после мержа |
| 9 | 2026-07-27T11:20:00+03:00 | INF-13 | рилс R-P1 v1.4.0 (53 с, RU/BE) + видеоблок на `/research/pension` (по `handoff/07_reels/MESSAGE_PUBLISH_REEL_PENSION.md`) | `a8e8a9b` (master) | Патч `publish_reel_pension_v1.4.0.patch` (sha256 `c439d80…` подтверждён) применён заранее, здесь — верификация (lint/vitest 30/30/build/pytest 14/14 зелёные) и коммит. `web/components/pension/ReelBlock.tsx`, ключи `reel.title`/`reel.caption` во всех 4 контент-файлах, `web/public/video/reel_pension_{ru,be}.{mp4,webp}` (перекодировка CRF18, ~3,2 МБ). Каноническая CBR-сборка (~79 МБ/файл) и GitHub-релиз — следующей записью. | ~3 мин, снят после мержа |
| 10 | 2026-07-27T11:25:00+03:00 | INF-13 | релизы: `artifact-pension-reel-v1.4.0` + закрытие пробела `artifact-pension-v1.0.0` | оба тега на GitHub | Канонический рендер (`.venv/bin/python tools/render_reel_pension.py --lang all`, ~79,2 МБ/файл) и `gh release create artifact-pension-reel-v1.4.0` с mp4×2 + постер RU. Контрольные суммы канонической сборки не совпали с эталонными из ТЗ (`ffmpeg` иной версии/платформы) — ожидаемо и заранее оговорено в ТЗ («сверяйте кадры, а не файл целиком, не повод останавливать публикацию»); pytest конвейера (14/14) подтверждает корректность кадров независимо от контейнера. Отдельно закрыт открытый пробел из отчёта INF-13 (раздел 6 декларировал публикацию пакета, тега/релиза не было) — `gh release create artifact-pension-v1.0.0` с `by-maps-pension-v1.0.0.zip`. | — (релизы, не мерж в master) |
| 11 | 2026-07-27T11:40:00+03:00 | INF-13 | хотфикс — ResizeObserver не срабатывает, карта/графики залипают на дефолтной ширине на мобильном | `3d120bd` (master) | Найдено при смоук-проверке рилса на проде (не связано с патчем рилса — файлы им не затронуты, баг предсуществующий). `PensionMap.tsx`/`LineChart.tsx`: первый колбэк `ResizeObserver` ни разу не срабатывает в части окружений (воспроизведено локально и на проде), SVG залипает на дефолте (640/388px) — реальная горизонтальная прокрутка на мобильном (29px). Исправлено синхронным чтением `el.clientWidth` в эффекте, `ResizeObserver` оставлен для последующих ресайзов. `e2e/pension.spec.ts`: отфильтрован безобидный шум WebKit (`video controls` PiP/AirPlay placard-иконки не грузятся в headless). Та же связка встречается ещё в 16 файлах сайта — вынесено отдельной задачей (не входит в эту сдачу). Проверено: tsc/vitest(30)/build/e2e(71/71) зелёные, checksum архива не изменился, overflow 29px → 0 (проверено и на проде). | ~5 мин, снят после мержа |

| 12 | 2026-07-28T02:14:49+03:00 | INF-15 | доработка WP-1/WP-2/WP-3 по `docs/notes/grid_ux_audit_2026-07-27.md` и `handoff/09_next_research/INF-15_grid_fix_order.md` | `6f86d79` (master, squash от `feature/inf15-grid`) | Раздел `/research/grid` перестал показывать пустую карту/прочерки на 60 из 76 прежних положений ползунка (B-1, дискретный ползунок по 16 узлам), переключатель метрики подключён (B-2: класс плотности — 46 новых кадров; метры сети на жителя — хороплет районов), клик по клетке больше не залипает на годе первого рендера (B-3), добавлены ▶ (B-4), коридор погрешности + автозум трека центра масс (B-5), легенда (B-6), 6 новых e2e-проверок (B-7), снят мёртвый код/утечка (B-8). Самостоятельно найден и исправлен баг вырождения трека центра масс в 1897-1959 годах (D-009) — трек также честно достроен прогнозом до 2050 года. Текст страницы дополнен («как читать карту», «главная находка», результат по дорожной сети, источники, чипы статуса утверждений), перевод BE. Версия пакета 1.0.0 → 1.1.0 (MINOR), `checks/expected_results.json` 20 → 26 записей, оба провалидированы. e2e 41/41 (grid.spec.ts) + 203/203 (полный набор, 1 flaky в несвязанном `urban.spec.ts`, не наш файл). | ~1 мин, снят этой же записью |

| 13 | 2026-07-28T09:22:00+03:00 | INF-15 | доработка v2 — режим «История», гигиена интерфейса, проверка варианта A (по `handoff/09_next_research/INF-15_grid_v2_brief.md`) | `04bc66e` (master, squash от `feature/inf15-grid`@`ff92fe4`) | Режим «История» (7 шагов, по умолчанию, deep-link `?story=N`, переключатель с режимом «Свободно», порт механики с `/research/nightlights`); гигиена (▶ отдельной кнопкой 46px, слитый слайдер вместо двух индикаторов, по-кнопочные RU-подсказки, блок «как читать карту» распределён по элементам). Данные для истории: гипотезы C-004 («правило пятисот», подтверждена точным независимым пересчётом) и C-005 (расстояние до магистрали, открытый вопрос) — `etl/grid_story.py`, `docs/preregistration/grid-v0.2.md`. Независимо перепроверена и подтверждена находка о внутрирайонной концентрации населения (доля района в топ-5 плотных клетках почти не менялась эмпирически 1975→2020, но прогноз даёт скачок к 2050 — самоусиление log-linear экстраполяции по клетке, не ошибка данных) — добавлена проверка G-9, честно не пройдена по конструкции (62/118 районов), см. `docs/decisions/INF-15.md` D-013. Версия пакета 1.1.0 → 1.2.0 (MINOR), `checks/expected_results.json` 26 → 51 запись, `build.py` пересчитывает новые метрики независимо из вендоренных `sources/raw/*`. e2e 77/77 (`grid.spec.ts`, включая новый BE-паритет тест на режим «История»), полный набор без регрессий в общих файлах. Приёмка — 10/10 пунктов раздела 10 брифа. | ~10 мин, снят этой же записью |

| 14 | 2026-07-28T12:20:00+03:00 | INF-15 | доработка v3 — реки, режим «История» 9 шагов, проверка вариантов A (документ «Полотно v3: готовый копирайтинг раздела и доработка демонстрационной карты») | `5b6c8be` (master, squash от `feature/inf15-grid`@`2839ee5`) | Режим «История» расширен с 7 до 9 шагов: новые шаги 3-5 (страна у воды, вода без дороги, матрица река×дорога — гипотезы C-006/C-007), «три пустоты» вместо «двух» на шаге 6 (добавлена полесская группа, автопоследовательность зумов). Слой рек (`etl/osm_rivers.py`, только реки ≥20 км в границах Беларуси) и три новых растровых оверлея. C-006 (растёт только пересечение реки и дороги) подтверждена направленно, `open_question`. **C-007** (максимум плотности в 2-5 км от реки, не у самой воды) — **ОПРОВЕРГНУТА** собственным заранее заданным критерием (доля населения у воды больше — крупные города стоят прямо на реке), текст шага 3 переписан без опровергнутого утверждения, не смягчён и не скрыт — `docs/decisions/INF-15.md` D-015/D-016. Свободный режим: 5 новых тумблеров слоёв, таблица всех 119 территорий с сортировкой/CSV, карточка клетки резолвит район. Версия пакета 1.2.0 → 1.3.0, `checks/expected_results.json` 51 → 72 записи. e2e 99/99 (`grid.spec.ts`, +18 новых тестов), полный набор 261/261 (1 flaky в несвязанном `urban.spec.ts`). Приёмка — 10/10 пунктов документа v3; 2 дефекта найдены и исправлены при самой проверке (магистрали не показывались на шаге 3, отсутствовала цветовая легенда матрицы). | ~15 мин, снят этой же записью |
| 15 | 2026-07-29T00:06:00+03:00 | INF-15 | публикация иллюстрированной статьи `/article/grid` (по `handoff/09_next_research/INF-15_article_site_publish_order.md`) | `61b7063` (master) | Новый маршрут `/article/grid`+`/be/article/grid` (loadContent+ContentDoc), `Markdown.tsx` — поддержка `![alt](src)` → figure/figcaption (первая картинка eager, остальные lazy), 20 иллюстраций сконвертированы в WebP (`tools/convert_article_images.py`, 200 КБ/кадр, итог 2.37 МБ), карточка-ссылка первой строкой в «Смежные материалы» на `/research/grid`, sitemap/og:image. Перед вставкой текст сверен с `grid.json`/`grid_story.json`/`expected_results.json` (вручную + 8 параллельных агентов) — найдено и исправлено 8 реальных ошибок в `ARTICLE_MEDIUM.md` (неверная арифметика, дважды реинкарнация опровергнутой C-007, ложная монотонность градиента дорог, ложная превосходная степень в матрице река×дорога, некорректная интерпретация независимой проверки на городах `n_rest=2`, преувеличенная «стократная» разница сети дорог из-за неисключённого Минска) — зеркально исправлены в `ARTICLE_LINKEDIN.md`/`POST_GRID.md` (не опубликованы) и в `tools/assets_grid.py` (баг был запечён в 3 иллюстрациях). Одна из этих ошибок уже была на проде (`grid-story.ru.md`/`.be.md`, шаг 4 режима «История») — исправлена тем же коммитом. D-017 в `docs/decisions/INF-15.md`. e2e: `web/e2e/article-grid.spec.ts` 30/30, полный набор 321/321 (1 flaky `urban.spec.ts`, несвязан). | ~10 мин, снят этой же записью |
| 16 | 2026-07-29T01:20:00+03:00 | INF-15 | боковое меню «Статьи» на /article/* + ссылка из основного эссе (по обратной связи пользователя после публикации статьи) | `224051a` (master, squash от `feature/inf15-grid`@`94c1871`) | Новый реестр `web/lib/articles.ts`, компонент `ArticlesMenu.tsx` + новый проп `ContentDoc.tsx::articlesNav` — список статей в той же боковой колонке, где раньше было только оглавление текущей страницы; текущая статья не ссылка. Подключено на 4 страницы (`/article`, `/article/grid` + BE), остальные content-страницы не затронуты. В `article.md`/`.be.md` добавлена ссылка на `/article/grid` в конце раздела «Перераспределение...». `SiteNav.tsx`: «Статья» → «Статьи». Работа сделана в ветке `feature/inf15-grid` (не в основном чек-ауте, как предыдущая сдача) — по прямому запросу пользователя навести порядок в git. D-018 в `docs/decisions/INF-15.md`. e2e 310/310 (`article-grid.spec.ts` расширен, полный набор, 0 flaky в этом прогоне). | ~10 мин, снят этой же записью |
| 17 | 2026-07-29T02:05:00+03:00 | INF-13 | аудит: устаревшая ветка `feature/inf13-pension-ux`@`f23af6d` — ложная тревога | `4b0fe70` (master, docs-only) | По запросу пользователя проверено состояние незамерженной ветки INF-13. `f23af6d` оказался устаревшим, более бедным дубликатом уже слитой и задеплоенной доработки (`6dc5803`, запись №6) — ветку не сбросили после первой сдачи. Прямое сравнение файлов (`PensionView.tsx` беднее на 9 строк, CSS-блок короче) и живая проверка на проде подтвердили: все находки отчёта `f23af6d` уже опубликованы. Решение — не мержить (внесло бы регресс), ветка сброшена на master локально. D-019 в `docs/decisions/INF-13.md`. Отдельного деплоя не требует — на проде ничего не меняется. | ~5 мин, снят этой же записью |
| 18 | 2026-07-29T02:40:00+03:00 | INF-13 | рилс R-P1 v1.5.0 — вёрстка в безопасной зоне 4:5, фикс кропа Facebook (по `handoff/07_reels/MESSAGE_UPGRADE_REEL_v1.5.md`) | `bbd235c` (master) | Найден готовый, но неисполненный патч (`upgrade_reel_pension_v1.5.0.patch`, sha256 сверен, накатывается на `665be2c` чисто) — публикация v1.4.0 в ленте Facebook обрезала кадр 9:16 до 4:5, теряя лого/тэглайн/адрес/год/плашку «ПРОГНОЗ». Вся смысловая вёрстка перенесена в безопасную зону (`tools/render_reel_pension.py`), содержание/хронометраж/числа не изменились. Применено и перепроверено: lint/vitest(30)/build чисто, `pytest test_pension_reel.py` 14/14, `e2e/pension.spec.ts` 71/71, полный набор pytest 331/331. `RELEASE_NOTES_REEL_PENSION.md` обновлён под v1.5.0. Далее — канонический рендер, тег `artifact-pension-reel-v1.5.0`, релиз, деплой. | ~10 мин, снят этой же записью |

## Активные замки

(пусто — нет удерживаемых замков)
