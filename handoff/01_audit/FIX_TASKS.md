# Задачи для группы разработки — релиз 1.0

Формат: приоритет · задача · файлы · критерии приёмки.
**P0 — блокирует релиз. P1 — сделать в релизе, если не сдвигает срок. P2 — следующие версии.**

---

## P0 — обязательные исправления

### T-01. Открыть публичный доступ к репозиторию GitHub
- **Проблема:** `https://github.com/sergeionlyart/BY_maps` → 404; ссылки на него — на каждой странице сайта, в docs и внутри всех zip-пакетов; git-теги релизов (`artifact-<slug>-vX.Y.Z`) внешне непроверяемы.
- **Действия:** сделать репозиторий публичным; убедиться, что запушены все теги (`git push --tags`) и ветка соответствует деплою. Если публичность невозможна к релизу — временно заменить ссылки на архив кода (zip на сайте) и убрать обещания «код на GitHub».
- **Приёмка:** анонимный запрос к репо и к 3 произвольным тегам возвращает 200; все ссылки с сайта на GitHub кликабельны из инкогнито-сессии.

### T-02. Починить `lib/research.test.ts` (красный CI-гейт)
- **Проблема:** тест ожидает ровно 9 исследований и паттерн файла `by-maps-<slug>-vX.Y.Z.zip`; после добавления 10-го (slug `ml`, файл `by-maps-mlchallenger-v1.0.0.zip`) 2 теста падают.
- **Действия:** обновить ожидание длины (10) и список кодов (INF-01…09 + ML); для проверки имени файла использовать поле пакета (например, `artifactSlug`), а не route-slug.
- **Приёмка:** `npm run test` зелёный локально и в CI; полный CI-прогон (pytest → vitest → tsc → build → artifacts validate) зелёный на релизном коммите.

### T-03. Синхронизировать версии прогноза в контенте
- **Проблема:** `web/public/content/methods/forecast.md` — заголовок «(v2026.3)», текст «жёстко v2026.3», футер ссылается на `by-maps-forecast-v1.2.0.zip`; фактически опубликованы forecast.json **v2026.4** и пакет **v1.3.0**.
- **Действия:** обновить все упоминания версии и ссылку на актуальный пакет; добавить абзац changelog v2026.4 (вероятностный веер). Рассмотреть (P2) генерацию версии/ссылки из `artifact.yaml`, чтобы исключить рецидив.
- **Приёмка:** вне блока changelog `grep -rn "v2026\.3\|forecast-v1\.2\.0" web/public/content/` не находит вхождений; страница методики прогноза ссылается на v1.3.0.

### T-04. Исправить версию в шапке `/research/aging`
- **Проблема:** `web/app/research/aging/page.tsx:15` — захардкожено «v1.0.2»; актуальный пакет v1.0.3; `methods/aging.md` также ссылается на v1.0.2.zip.
- **Действия:** брать версию из `web/lib/research.ts` (единый источник), поправить ссылку в aging.md.
- **Приёмка:** шапка и ссылки показывают v1.0.3; версия на странице совпадает с реестром research.ts (желательно закрепить unit-тестом).

### T-05. Починить инструкцию воспроизведения в README
- **Проблема:** `pip install shapely pytest` → `ModuleNotFoundError: yaml` (3 файла тестов падают на сборе); формулировка «без внешних зависимостей, кроме shapely» неточна.
- **Действия:** README: `pip install shapely pytest pyyaml`; уточнить формулировку (ядро — stdlib + shapely + pyyaml; numpy/rasterio — только для nightlights-extract, pdfplumber — для wpf1); привести README и CI к одному списку. Идеально — добавить `requirements.txt`.
- **Приёмка:** на чистой машине последовательность из README проходит до зелёного pytest без ручных доустановок.

### T-06. Устранить расхождение «49 пар» моногородов
- **Проблема:** `docs/SOURCES.md` и `methods/monotowns.md` заявляют 49 пар; в `data/raw/monotowns/registry.json` и в расчётах — 46.
- **Действия:** выяснить судьбу 3 пар (исключены при курации?). Либо вернуть недостающие записи, либо исправить тексты на «46» с примечанием о критериях включения.
- **Приёмка:** число в текстах = числу в registry.json = числу в `monotowns.json`; тест-инвариант на совпадение.

### T-07. Разрешить противоречие «площадь/плотность Минска»
- **Проблема:** METHODOLOGY.md и `methods/map.md`: «полигон Минска ~216 км², плотность завышена»; в `data.json` area Минска = **409,5 км²** (официальная ~349 км²) — оговорка инвертирована относительно поставляемых данных.
- **Действия:** проверить фактический полигон (geoBoundaries) и вычисление площади; если 409,5 верно для текущего полигона — переписать оговорку («полигон шире официальной границы, плотность занижена»); если полигон ошибочен — исправить и пересобрать. Проверить, что карта/карточка Минска используют согласованное значение.
- **Приёмка:** площадь в data.json, текст оговорки и official-справочник (~349 км²) согласованы между собой; описание направлений смещения соответствует данным.

---

## P1 — желательно в этом релизе

### T-08. Статический рендер `/methodology`
Сейчас — клиентский fetch двух .md (единственная страница без контента в HTML; SEO/архив/без-JS). Перенести чтение markdown в серверный компонент при сборке. **Приёмка:** `curl` страницы содержит текст методики.

### T-09. Внешние контрольные суммы пакетов
Сгенерировать `<pkg>.zip.sha256` рядом с каждым zip (или единый `checksums.txt` на /artifacts) + строка «как проверить». **Приёмка:** `sha256sum -c` проходит для всех опубликованных пакетов.

### T-10. Сноска об определении «городского» в 1897
В панели урбанизации/README: 9,7% — «официальные города» (~10% акад. оценок); ретроспектива Белстата с местечками — 13,5%. **Приёмка:** сноска видна в UI рядом с 1897 или в методике панели.

### T-11. Мелкие фактические правки текстов
1) `methods/map.md`: смягчить «единственный публичный набор…»; 2) `methods/shocks.md`: выверить топ по доле идиша (добавить Докшицы 75,9%, убрать Бобруйск из «60–74%»); 3) бэктест-формулировка: худшая — Минская обл. (APE 5,37), Минск переоценён +1,42%; 4) указать издание ежегодника для 1950 (7 709 тыс.); 5) единообразное цитирование «базового 2075» (5 970 — узел 2075 против 5 912 — узел 2076) с пояснением. **Приёмка:** тексты соответствуют данным пакетов.

### T-12. Актуализировать размеры пакетов на /artifacts
Расходятся на единицы КБ (monotowns, mlchallenger, forecast). Генерировать из файловой системы/манифеста при сборке. **Приёмка:** размеры на странице = фактическим.

---

## P2 — следующие версии

- **T-13.** Единый источник версий: версия/имя пакета в текстах методик подставляется из `artifact.yaml` на сборке (устраняет класс ошибок C-3/C-4-типа).
- **T-14.** validation.md: подписать различие WPP high/low-2050 «вариант» vs «интервал» (7 996/6 907 против 8 260/6 750).
- **T-15.** Решить судьбу старых версий пакетов в деплое (~28 МБ): подписанная «история релизов» или внешний архив.
- **T-16.** Зеркалирование хрупких источников (census.belstat.gov.by; незаархивированные вторичные источники WP-F3, помеченные в adjustment.md §7).
- **T-17.** Новые разделы сайта и двуязычие RU/BE — см. `../02_site/SITE_STRUCTURE.md` (контент готов в `../03_content/`).

---

## Порядок работ (рекомендация)

1. T-01 (доступ к репо) — независим, разблокирует внешнюю проверку.
2. T-02 (тест) → зелёный CI.
3. T-03…T-07 (контент/данные) одной серией + прогон полного CI.
4. T-08…T-12 по остатку времени.
5. Деплой, smoke-тест по чек-листу §2 аудита, релизный тег.
