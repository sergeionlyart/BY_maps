import type { Metadata } from 'next';
import Link from 'next/link';
import MLChallengerView from '@/components/MLChallengerView';
import AuthorCard from '@/components/AuthorCard';
import { authors } from '@/lib/seo';

export const metadata: Metadata = {
  authors,
  title: 'ML-челленджер: ищет ли машина ошибку в модели CCR — Население Беларуси',
  description:
    'Градиентный бустинг на экзогенных признаках ≤2019 против остатка структурной модели CCR относительно официальной оценки-2026: перестановочно-гейтированная диагностика, а не прогноз. Сигнал есть (OOF R²=0.33, p=0.005) и тянет его миграция.',
};

export default function MLChallengerPage() {
  return (
    <div className="page page-wide">
      <div className="page-breadcrumb">
        <Link href="/research">Исследования</Link> · ML · v1.0.0
      </div>
      <h1>ML-челленджер: оставляет ли CCR систематическую ошибку, которую видно из ковариат</h1>
      <p className="page-lead">
        Структурная модель CCR (cohort-change ratio) переносит район из переписи
        в оценку чисто демографически — по возрастным когортам, без миграции.
        Мы берём её остаток относительно ОФИЦИАЛЬНОЙ оценки-2026,
        e = ln(факт/CCR), и спрашиваем машину: предсказуема ли эта ошибка из
        признаков, датированных ≤2019 (зарплаты, транспортная доступность,
        ночная светимость, миграционное сальдо, возрастная структура)? Ответ,
        загнанный под перестановочный нуль, — да: вне выборки объясняется треть
        дисперсии остатка (OOF R²=0.33 при p=0.005), и почти весь устойчивый
        сигнал несёт миграционное сальдо 2015–2019. Это значит не «CCR
        неверна», а что CCR систематически недо-использует миграцию, которую
        официальная оценка уже учла. Диагностика на одном переписном окне —
        не конкурирующий прогноз; гонка MAPE внизу намеренно понижена в ранге.
      </p>
      <MLChallengerView />
      <AuthorCard variant="compact" lang="ru" />
    </div>
  );
}
