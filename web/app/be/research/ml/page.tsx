import type { Metadata } from 'next';
import MLChallengerView from '@/components/MLChallengerView';
import ResearchShell from '@/components/ResearchShell';
import { authors } from '@/lib/seo';

export const metadata: Metadata = {
  authors,
  title: 'ML-чэленджар: ці шукае машына памылку ў мадэлі CCR — Насельніцтва Беларусі',
  description:
    'Градыентны бустынг на экзагенных прыкметах ≤2019 супраць астатку структурнай мадэлі CCR адносна афіцыйнай ацэнкі-2026: перастановачна-гейтаваная дыягностыка, а не прагноз. Сігнал ёсць (OOF R²=0.33, p=0.005) і цягне яго міграцыя.',
  alternates: { languages: { ru: '/research/ml', be: '/be/research/ml' } },
};

export default function MLChallengerPage() {
  return (
    <ResearchShell
      code="ML"
      version="v1.0.0"
      title="ML-челленджер: оставляет ли CCR систематическую ошибку, которую видно из ковариат"
      lead="Структурная модель CCR (cohort-change ratio) переносит район из переписи в оценку чисто демографически — по возрастным когортам, без миграции. Мы берём её остаток относительно ОФИЦИАЛЬНОЙ оценки-2026, e = ln(факт/CCR), и спрашиваем машину: предсказуема ли эта ошибка из признаков, датированных ≤2019 (зарплаты, транспортная доступность, ночная светимость, миграционное сальдо, возрастная структура)? Ответ, загнанный под перестановочный нуль, — да: вне выборки объясняется треть дисперсии остатка (OOF R²=0.33 при p=0.005), и почти весь устойчивый сигнал несёт миграционное сальдо 2015–2019. Это значит не «CCR неверна», а что CCR систематически недо-использует миграцию, которую официальная оценка уже учла. Диагностика на одном переписном окне — не конкурирующий прогноз; гонка MAPE внизу намеренно понижена в ранге."
    >
      <MLChallengerView />
    </ResearchShell>
  );
}
