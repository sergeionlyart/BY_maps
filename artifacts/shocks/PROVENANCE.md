# Происхождение данных (INF-09, v1.0.0)

```
Демоскоп (перепись-1897)          Редакторская хронология событий
  родной язык + вероисповедание     (беженство 1915, граница 1921,
  по городам губерний Беларуси      репрессии, ВМВ, Холокост,
        │                           «неперспективные деревни», 1991)
        ▼                                 │ каждое с проверяемым URL
  census_lang.json / census_relig.json    ▼
        │                           events.json
        │                                 │
        │   Yad Vashem/USHMM/энц. гетто    │
        │   -> holocaust.json (доля-1897 + │
        │      население 1939/1959)        │
        ▼                                 ▼
  etl/shocks.py (стандартная библиотека):
  национальный ряд из data.json (обрыв ВМВ 1940->1950);
  доля евреев города = идиш / всё; население Холокост-городов из
  рядов проекта; события упорядочены по году
        ▼
  web/public/data/shocks.json
```

Все шаги детерминированы; сетевых вызовов в run.sh нет. sha256 сырья и
провенанс — `sources/registry.csv`; построчные источники событий и
Холокост-городов — в самих json.
