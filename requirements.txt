# Ядро воспроизведения BY Maps: сборка данных, инварианты (155 тестов),
# байт-воспроизводимость всех 11 пакетов артефактов. Точь-в-точь как ставит CI.
shapely
pytest
pyyaml

# Опционально (НЕ нужны для проверки/воспроизведения — данные завендорены):
#   numpy, rasterio  — только для ПЕРЕСОЗДАНИЯ растровых экстрактов INF-08
#                      (nightlights) из исходных композитов VIIRS (~ГБ)
#   pdfplumber       — только для парсинга PDF-томов переписей на этапе WP-F1
