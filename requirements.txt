# Ядро воспроизведения BY Maps: сборка данных, инварианты (225 тестов),
# байт-воспроизводимость всех 14 пакетов артефактов. Точь-в-точь как ставит CI.
shapely
pytest
pyyaml
# видеоконвейер рилсов (R3): etl/tests/test_nightlights_reel.py импортирует
# video/nightlights-reel/render.py — нужны numpy и pillow (лёгкие колёса)
numpy
pillow

# Опционально (НЕ нужны для проверки/воспроизведения — данные завендорены):
#   rasterio, scipy, osmium — только для ПЕРЕСОЗДАНИЯ растровых экстрактов
#                      INF-08 (nightlights) и INF-12 (urban-overhang) из
#                      исходных композитов/тайлов/PBF (~ГБ)
#   pdfplumber       — только для парсинга PDF-томов переписей на этапе WP-F1
